import type { Metadata } from "next";
import { notFound } from "next/navigation";
import { EditorialGallery, FullBleedMedia, NextProject, PanoramaPreview, ProcessTimeline, ProjectCredits, ProjectHero, ProjectIntroduction, ProjectMetadata, TechnicalLayers } from "@/components/ProjectModules";
import { projects } from "@/content/site";
export function generateStaticParams() { return projects.map(({ slug }) => ({ slug })); }
export async function generateMetadata({ params }: { params: Promise<{ slug: string }> }): Promise<Metadata> { const { slug } = await params; const p = projects.find(x => x.slug === slug); return p ? { title: p.title, description: p.summary, alternates: { canonical: `/work/${p.slug}` } } : {}; }
export default async function ProjectPage({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const p = projects.find(x => x.slug === slug); if (!p) notFound(); const creativeWork = { "@context": "https://schema.org", "@type": "CreativeWork", name: p.title, description: p.summary, creator: { "@type": "Person", name: "Alessandro Bobbio Russian" } }; return <main className="project-page"><ProjectHero project={p} /><ProjectIntroduction project={p} /><ProjectMetadata project={p} /><FullBleedMedia project={p} /><EditorialGallery project={p} /><ProcessTimeline project={p} /><TechnicalLayers project={p} /><PanoramaPreview project={p} /><ProjectCredits project={p} /><NextProject project={p} /><script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(creativeWork).replace(/</g, "\\u003c") }} /></main>; }
