"use client";
import { useState } from "react";
import { capabilities } from "@/content/site";

export function ConnectedPractices() { const [active, setActive] = useState(0); const items = [{ title: "Architectural Design", text: "Luxury interiors, Total Living, premium kitchens and bespoke furniture—developed through visualization, technical coordination and delivery." }, { title: "Bio-Design", text: "Productive architecture, hydroponic and aeroponic systems, bioclimatic thinking and digital twins for resilient environments." }]; return <section className={`connected active-${active}`}><div className="connected-visual" aria-hidden="true"><div className="system-lines" /></div><div>{items.map((x, i) => <button key={x.title} className={active === i ? "active" : ""} onClick={() => setActive(i)} onFocus={() => setActive(i)}><span>0{i + 1}</span><strong>{x.title}</strong><p>{x.text}</p></button>)}</div></section>; }
export function CapabilityExplorer() { const [active, setActive] = useState(0); const item = capabilities[active]; return <div className="capability-explorer"><div role="tablist" aria-label="Capability stages">{capabilities.map((x, i) => <button role="tab" aria-selected={active === i} key={x.stage} onClick={() => setActive(i)}>{x.stage}</button>)}</div><div className="capability-detail" role="tabpanel"><p>{item.value}</p><ul>{item.items.map(x => <li key={x}>{x}</li>)}</ul><b aria-hidden="true">0{active + 1}</b></div></div>; }
